import { useAuth } from '@/context/AuthContext';

// AdBanner component removed - premium users no longer see ads, 
// and the placeholder has been removed for a cleaner UI
export const AdBanner = () => {
  // Component disabled - return null
  return null;
};
